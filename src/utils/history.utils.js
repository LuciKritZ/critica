import { createBrowserHistory } from 'history';

// History object to be shared across the app
// eslint-disable-next-line import/prefer-default-export
export const appHistory = createBrowserHistory();
